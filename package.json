{
  "name": "cloudformation-deploy-action",
  "version": "1.0.0",
  "description": "GitHub Action to create or update a CloudFormation stack using AWS CLI",
  "main": "dist/action.js",
  "scripts": {
    "build": "ncc build index.js --minify --out dist"
  },
  "author": "Benoy Bose <benoybose@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@vercel/ncc": "^0.36.1",
    "yaml-cfn": "^0.3.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
